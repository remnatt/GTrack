python GTrack.py --api --botAs 'GTracker' --incommit --readme=true --bold \
--licsensetype 1 --gitignore=false --extrafile 'header' \
--extrafiletype py --content '# this is a header \n import os # this is a import' \
--customcommitmessage ':green checkmark: test commit' --vmode

  "yet to be confirmed or put back into work."
